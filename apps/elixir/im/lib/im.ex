defmodule IM do
  @moduledoc """
  IM 主服务根模块（占位，P0-05 后由分层模块替代）。
  """

  @doc """
  占位函数，用于验证 `mix test` 与 doctest。

  ## 示例

      iex> IM.hello()
      :world

  """
  @spec hello() :: :world
  def hello do
    :world
  end
end
