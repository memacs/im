# IM 主服务（Elixir）

Phoenix + OTP 主 IM 服务。`lib/` 布局见 [project-structure.md](../../../docs/implementation/elixir/project-structure.md)。

**状态**：P0-01 完成；`mix compile` / `mix test` 可用。

```bash
cd apps/elixir/im && mix compile && mix test
```

部署：`deploy/elixir/im/`。
