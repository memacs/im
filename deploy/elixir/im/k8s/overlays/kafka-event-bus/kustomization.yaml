apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: im-dev

# 本地全栈 + Redpanda + Event Bus 旁路（按需）
resources:
  - ../local
  - redpanda.yaml

patches:
  - path: configmap-patch.yaml
    target:
      kind: ConfigMap
      name: im
