apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: im-dev

# 本地全栈：依赖 + IM Release（与线上同一 Dockerfile / Deployment 形态）
resources:
  - ../../base
  - ../../im

images:
  - name: im
    newName: im
    newTag: local
