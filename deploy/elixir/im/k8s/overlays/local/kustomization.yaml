apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: im-dev

# 本地全栈：依赖（含 Redpanda）+ IM Release
resources:
  - ../../base
  - ../../im

images:
  - name: im
    newName: im
    newTag: local
