apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: im-dev

# 本地全栈：依赖 + IM Release + Redpanda（Kafka 旁路默认开，见 im/configmap.yaml）
resources:
  - ../../base
  - ../../im
  - redpanda.yaml

images:
  - name: im
    newName: im
    newTag: local
