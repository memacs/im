apiVersion: v1
kind: Secret
metadata:
  name: im-runtime
  namespace: im-dev
  labels:
    app: im
type: Opaque
stringData:
  # 仅 im-dev 本地占位；生产由外部 Secret 管理注入，禁止提交真实密钥
  SECRET_KEY_BASE: "dev_only_secret_key_base_minimum_64_characters_long_for_phoenix_xxxxxxxx"
  RELEASE_COOKIE: "im_dev_release_cookie"
