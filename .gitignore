# 仓库根忽略规则（monorepo：apps/ 各子项目另有自己的 .gitignore）

# ── 操作系统 ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Elixir / Mix ─────────────────────────────────────────────────────────────
**/_build/
**/deps/
**/cover/
**/doc/
**/.elixir_ls/
erl_crash.dump
*.ez
*.beam

# ── Protobuf 生成代码（由 protoc 生成，不入库）────────────────────────────────
/gen/

# ── vendor skill（自带 .git 的上游 clone，暂不入库；见 .agents/skills/README.md）
# 待决：改为 git submodule，或删除其 .git 目录后作为普通文件入库
.agents/skills/kubernetes-skill/

# ── Node / 前端（apps/web/im-console）─────────────────────────────────────────
**/node_modules/
**/dist/
**/.vite/

# ── 密钥与本地环境（生产 Secret 由外部注入，禁止入库）─────────────────────────
.env
.env.*
!.env.example
*.pem
*.key

# ── 编辑器 ───────────────────────────────────────────────────────────────────
.idea/
*.swp
